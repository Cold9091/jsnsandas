# Environment Variables for JSM SANDA
NODE_ENV=production

# Optional: Database URL (if using external database)
# DATABASE_URL=your_database_connection_string

# Optional: Analytics (Google Analytics, etc.)
# VITE_GA_TRACKING_ID=your_google_analytics_id

# Optional: Email service (if implementing contact forms)
# EMAIL_SERVICE_API_KEY=your_email_service_key

# Vercel automatically provides these:
# VERCEL_URL=your_deployment_url
# VERCEL_ENV=production